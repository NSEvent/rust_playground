# rust_playground

# Setup rust environment in current directory
cargo init

# Build
cargo build

# Build and run
cargo run

# Build optimized
cargo build --release

# Build and run optimized
cargo run --release
